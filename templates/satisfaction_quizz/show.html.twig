{% extends 'base.html.twig' %}

{% block title %}SatisfactionQuizz{% endblock %}

{% block body %}
    <h1>SatisfactionQuizz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ satisfaction_quizz.id }}</td>
            </tr>
            <tr>
                <th>BusinessContact</th>
                <td>{{ satisfaction_quizz.businessContact ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>SatisfactionNote</th>
                <td>{{ satisfaction_quizz.satisfactionNote }}</td>
            </tr>
            <tr>
                <th>ContactNumber</th>
                <td>{{ satisfaction_quizz.contactNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('satisfaction_quizz_index') }}">back to list</a>

    <a href="{{ path('satisfaction_quizz_edit', {'id': satisfaction_quizz.id}) }}">edit</a>

    {{ include('satisfaction_quizz/_delete_form.html.twig') }}
{% endblock %}
