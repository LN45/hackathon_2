{% extends 'base.html.twig' %}

{% block title %}SatisfactionQuizz index{% endblock %}

{% block body %}
    <h1>SatisfactionQuizz index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>BusinessContact</th>
                <th>SatisfactionNote</th>
                <th>ContactNumber</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for satisfaction_quizz in satisfaction_quizzs %}
            <tr>
                <td>{{ satisfaction_quizz.id }}</td>
                <td>{{ satisfaction_quizz.businessContact ? 'Yes' : 'No' }}</td>
                <td>{{ satisfaction_quizz.satisfactionNote }}</td>
                <td>{{ satisfaction_quizz.contactNumber }}</td>
                <td>
                    <a href="{{ path('satisfaction_quizz_show', {'id': satisfaction_quizz.id}) }}">show</a>
                    <a href="{{ path('satisfaction_quizz_edit', {'id': satisfaction_quizz.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#<a href="{{ path('satisfaction_quizz_new') }}">Create new</a>#}
{% endblock %}
